# KATSEYE Creative Site Starter

A clean Next.js starter for a highly visual, scroll-driven KATSEYE-inspired creative website.

## Run it

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Next step

Replace the hero placeholder with your exported Figma PNG artwork. If your design has multiple layers, export each layer separately with transparent backgrounds so they can be animated independently.
